<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>AquaNet Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta http-equiv="refresh" content="30"> </head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-primary mb-4">
        <div class="container-fluid">
            <span class="navbar-brand mb-0 h1">💧 AquaNet: Публичный мониторинг</span>
            <span class="text-white">Текущий дефицит воды: <strong><%= deficit %>%</strong></span>
        </div>
    </nav>

    <div class="container">
        <% if (Object.keys(villages).length === 0) { %>
            <div class="alert alert-warning">Нет данных о фермерах. Зарегистрируйтесь через бота!</div>
        <% } %>

        <div class="row">
            <% Object.keys(villages).forEach(function(villageName) { %>
                <div class="col-md-6 mb-4">
                    <div class="card shadow-sm">
                        <div class="card-header bg-success text-white d-flex justify-content-between">
                            <h5 class="mb-0">🏡 Айыл: <%= villageName %></h5>
                        </div>
                        <div class="card-body p-0">
                            <table class="table table-striped mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Фермер</th>
                                        <th>Культура</th>
                                        <th>Полив (мин)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% villages[villageName].forEach(function(farmer, index) { %>
                                        <tr>
                                            <td><%= index + 1 %></td>
                                            <td><%= farmer.name %></td>
                                            <td><%= farmer.crop %></td>
                                            <td><strong><%= farmer.duration %> мин</strong></td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>